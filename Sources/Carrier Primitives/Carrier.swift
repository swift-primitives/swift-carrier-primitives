// Carrier.swift
// Namespace for the Carrier family of abstractions.
//
// The `Carrier<Underlying>` super-protocol is still research-stage — its
// final shape will be driven by the experiments in `Experiments/` and the
// design analysis in `Research/`. This file currently declares only the
// namespace placeholder so that the package has a shipping target.

/// Namespace for the Carrier family — a super-abstraction over
/// phantom-typed value wrappers.
///
/// See `Research/capability-lift-pattern.md` for the full design
/// analysis. The protocol itself is intentionally absent until its
/// shape converges through experimentation.
public enum Carrier {}
